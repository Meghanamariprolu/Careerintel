export const metadata = {
    title:"Dashboard",
    description:"Your CareerIntel Command Center — view roadmaps, track progress, and access AI-powered career tools.",
};

export default function DashboardMetaLayout({ children }) {
    return children;
}
